def Population():
    def __init__ (self, size, parents, score, best):
        self.size = size 
        self.parents = parents
        self.score = score 
        self.best = best

    def create_init_pop(size, cities):
        inital_population = [] 
        
        # create array of random variables with size of n + 1
        # set first and last index to one start point 
        

        return inital_population